import React from 'react';
import Button from '../components/common/Button';

const Auth = () => {
  return (
    <div>
      <Button type='button' disabled={false}>
        test
      </Button>
    </div>
  );
};

export default Auth;
