import React from 'react';
import Button from '../components/common/Button';

const Main = () => {
  return (
    <div>
      <Button type='button' disabled={false}>
        test
      </Button>
    </div>
  );
};

export default Main;
